Installation
============

Run the seabreeze*.msi file to install seabreeze libraries and driver
Install anaconda python 2.7 if not installed.
Open the Python 2.7 prompt and install lmfit and pyqtgraph using pip
	e.g. pip install lmfit

Run program via python OOSpec.py

